# Changelog

## [Unreleased]

### Features
- Linux 适配：插件不再仅限 Windows（`os` 增加 `linux`、`cpu` 增加 `arm64`）。桌面浮窗运行时改为按平台/架构解析（vendor 目录、二进制名、zip 名、跨平台解压），Electron 缺失时仍回落页面内宠物；桌面窗 DPI 注册表读取仅在 Windows 生效，其它平台回退 Electron screen API。

### Fixes
- 桌面离线打开 DSH 网页：通过 `ctx.connection.authenticatedUrl` 带上 0.1.2 进程 token；桌面模式要求 DSH `>= 0.1.2-alpha.1`。
- fnOS/TRIM gateway 前缀路由（`/app/<id>/`）支持：dsh web 挂载在前缀下时，宿主桥接只改写 HTML 静态 `src`/`fetch`/`EventSource`/`<script src>`，运行时 JS 赋值的 `img.src`（贴纸、双击画画 pics、气泡卡 favicon）不被改写，请求落到 NAS 根路径 404 → 宠物贴图消失（只剩文字气泡）、气泡吞掉 pointer 事件导致拖不动。现从本 bundle 的 `<script>` 加载路径检测前缀（回退页面 path），所有运行时绝对路径带前缀；直连模式（无前缀）行为不变。

## [0.3.6] — 2026-08-22

### Features
- 牌叠第二层改为无内容的假背板：仅展示 `+N`，不再渲染第 2 名会话的文字与图标——同级会话的 updatedAt 轮转不会造成背景卡闪换；前两名滞回让顶层卡宽度在流式刷新下保持稳定。
- 点击背板按当帧排序动态跳转第 2 名会话（两端一致）；桌面点气泡卡/图标跳转对应对话，完成卡顺带消除提醒，与网页端行为完全对齐。
- 无网页客户端在线时点击桌面气泡卡：宿主暂存该动作并在下一个网页订阅者接入时重放直达；同时用系统浏览器打开 DSH 页面兜底。审批类动作时效性强，永不暂存重放。
- 网页端当前会话上报：宿主随快照带出，桌面牌叠置顶与自动 ack 感知用户正在看的对话；页面卸载时经 sendBeacon/keepalive 清空，宿主侧附 10 分钟 TTL 防页面崩溃残留。
- 排序层级升级：审批 > 等待回答（ask_user_question）> 完成卡 > attention > 当前会话 > 状态优先级 > 更新时间；排序逻辑抽取为共享模块（session-order.cjs），桌面与网页同一份实现。
- 桌面窗高缩放屏适配：force-dsf=1 使窗口 bounds／光标／workArea 与 OS 物理像素 1:1 直通，再按注册表 AppliedDPI 读出的真实系统缩放 R 对页面 zoom 与窗口尺寸统一补偿，桌宠物理尺寸与网页一致；拖拽改为主进程闭环绝对定位，消除增量式移动的持续漂移；绘画小窗同步缩放。
- 桌面右键菜单显示当前心情文字；工具提示措辞与网页端统一。
- 桌面右键菜单按需扩窗（menu-expand/menu-restore）：落点平时锚角色（与网页紧凑观感一致），菜单与气泡相交才改走包围盒避让；扩窗包住菜单+光晕，优先向右/下长（原点不动），贴右才向左扩并用 dx 把 `.pet` 顶左锚层推回。
- 审批「允许一次」没有网页客户端接收时，改为气泡堆叠上方弹出 toast 提示（Remielle 风格随机文案、下方淡入上方回落动画后自动消失），取代写入终端的 `console.warn`。

### Fixes
- 圆点短提示贴左会拆成「点击看余 / 额呀~」：先 nowrap 按自然宽展开，超出可见 maxW 再换行，盒子可向空侧偏置，仍钳在 24px 光晕内。
- 牌叠假背板悬停无文案：改为「点击去看 工作区 · 对话标题 哦~」（无方括号）；标题来自 `session/title` / `sessionTitle.get` / 网页 `sessions.list`。
- 文档与余额控制器仍写「单击宠物弹出余额、5 秒回落」：改为圆点/滚轮翻页且停在当前页；删掉无调用方的 `click` / `enterBalance` / 5 秒定时器。README 补一句排序滞回仅稳定前两名。
- 自绘 tip 钳位算法网页/桌面各一份：抽到 `src/pet-tip.cjs` 的 `layoutPetTip`；桌面 `showPetTip` 仍先按窗口再按 `getWorkArea` 收紧。
- `pnpm test` / `check` 漏掉 `pet-tip`；无 bundled Electron 时跳过「prefer bundled」单测。
- 桌面按住握拳有时未松就变回张手：`mousemove` 用 `buttons` 猜松手，透明窗上按住也会掉成 0。改为 pointer 捕获，只信 `pointerup` / `pointercancel`（窗外松开仍能收尾）。
- 气泡左侧翻页圆点仍用原生 title，悬停会和卡片自绘提示叠在一起：圆点改为空 title 截断祖先；悬停改走同一套自绘气泡（状态页「点击看余额呀~」、余额页「点击回状态呀~」），离开圆点仍在卡片上时切回卡片提示。圆点文案与离开路由抽到 `src/pet-tip.cjs`，网页/桌面共用。
- 网页余额页圆点露出边框时冒出 dock 的「拖动我 · 点击互动 · 右键菜单」：余额气泡/圆点写空 title，不再继承。
- 卡片固定悬停文案改成蕾米埃尔语气（「点击跳到这里看一下~」等；审批请求全文仍用第二行原文）。
- 网页自绘悬停提示在深色壳下仍是浅粉底：补 `body[data-ds-dark-theme] .rm2-pet-tip`，与气泡/菜单同色。
- 贴屏幕边时提示框出屏、左右光晕被切：定位改为钳在窗口∩工作区可见矩形内并留出 24px 光晕；桌面再按 `getWorkArea` 收紧，避免窗体探出屏幕时文字落在屏外。网页贴边改为与桌面相同的收窄换行（相对卡片居中），不再整块滑开。
- 桌面边缘光晕随设备 DPI 膨胀、且比网页更大更亮：阴影按 `--rm2-ui-zoom` 反缩放；粉影改负 spread + 更低透明度，深色壳改实色底，避免透明 HWND 上半透明粉 Bloom 成亮边。tip 不再用网页 8/24 大粉影。
- 审批悬停 title 与气泡第二行同一套行首项目符号规范化。
- 清掉 #8 合并残留：网页端重复声明的 `BUBBLE_TITLE_MS`、桌面端已无读取的 `lastBubbleMood/Text/Detail`。首行仍是同贴纸 2 秒锁，等待/出错/完成立即更新。
- 审批气泡悬停提示改为第二行全文（工作区 · 请求 preview）：方框宽度会 CSS 省略，原生 title 才能读到具体请求；「允许一次」说明仍在勾号 aria-label。
- 网页宠物按下缺少握拳光标；桌面虽有 grabbing，但快照 `apply()` 每次 `syncCursor()` 无条件打回 grab，按住未松就会变回张开的手。两端按住期间保持 grabbing，松开再恢复。
- 宿主 `states()` 与快照 `sessions[]` 改走 `session-order.compareSessions`（审批 > 提问 > 完成卡 > attention > 当前会话 > 状态 > 时间），不再与客户端各排各的。
- 出错停止后粉圈提醒看过也不消：`turn/end {kind:'error'}` 写成耐久 ERROR，打开对话/点气泡只跳转、没有已读。现与完成绿点同语义——打开该会话或失败时已在该对话里，则收成 IDLE，牌叠不再 attention；审批/提问与工具脉冲不动。
- 桌面右键菜单空旷处仍上移挡住气泡、开合瞬移、贴右不翻到左侧、光晕被窗边竖切：高度不再向上改 y；贴右按工作区向左扩窗。透明 HWND 上同样的大粉 box-shadow 会整圈开成光晕，改为中等粉光加短暗底影。
- 桌面右键贴屏幕右侧开合左右抽动：向左扩窗会移动 HWND 原点。现由主进程在原点移动时 `setOpacity(0)`，临时解开 `resizable` 后 `setContentBounds`（避免 Windows 忽略 x），先搬窗再清 shift；重申 bounds 放在隐藏期内，揭回前只等约 16ms，避免可见时再写 bounds 造成闪动。
- 切换到桌面模式时宿主终端多一个空行：Electron 子进程 `stdio: inherit` 把 Chromium 启动时的空白输出灌进控制台。改为 pipe，丢掉纯空白块，有内容的日志仍转发。
- 网页对话气泡边缘光晕大于右键菜单（顶卡 10/28 vs 菜单 8/24）：气泡/顶卡/牌叠统一为与菜单相同的 `0 8px 24px rgba(190,70,110,.22)`。
- 网页右键贴底上移挡住气泡：两端统一为平时锚角色，菜单矩形与气泡相交才改走包围盒（上缘与气泡顶齐、落在气泡外侧）。
- 高缩放屏上桌宠仍偏小一半：setZoomFactor 在 loadURL 之前调用会被导航重置回 1，页面从未放大、图案只剩一半大，拖动自校准随之测出错误系数致位移翻倍乱飞——移至导航完成后设置；并废弃早期「bounds = 物理×R 镜像世界」定标（系 DPI-unaware 测量进程被 Windows 按 ÷R 虚拟化读数造成的假象），Per-Monitor-V2 感知进程实测确认 force-dsf=1 下全部坐标 1:1 物理直通，zoom 因子由 R² 纠正为 R；另以 show 后重申完整尺寸兜底构造期 fit 钳制。
- GUI 强行终止会话后桌宠永久卡在「分析阶段」：DSH 强杀轮次时不向 live 事件总线补发 turn/end（仅冷读日志时 repair），插件侧事件流戛然而止——新增 turn 悬挂看门狗（每 30 秒扫描），THINKING/WORKING 超 3 分钟无任何事件即合成 aborted 收尾回 IDLE「已停止」；等待确认/等待回答可合法久等，不误杀。
- 打开 DSH 网页时右下角宠物闪现一下后消失：初始渲染改为快照驱动的显隐，挂载即隐藏。
- 桌面「待命中」占位卡可被点击：误广播打开不存在的会话、无网页在线时还会弹出浏览器——补齐与网页端一致的守卫。
- 桌宠窗口常驻 SSE 订阅曾让 session-action 永远“已送达”，暂存兜底形同虚设——订阅者区分 pet/web，delivered 只计网页端。
- 当前会话完成卡的自动 ack 提升到数据层全量检查：完成卡被审批压到背板之下时同样生效（此前仅顶层卡）。
- 重放帧写入失败时回滚暂存，动作不再静默丢失；success 文案池三处内联/引用增加防漂移测试。
- 桌面气泡卡光标与网页对齐：整卡可点击跳转后工作卡同样显示 pointer，移除「桌面卡片不跳转」时代（0.3.4）的遗留光标判定。
- 待机占位卡悬停仍提示「点击跳转到此对话」：占位卡有 activate 守卫不可交互，title 改为置空（两端对齐）。
- 深色模式右键菜单整体回退浅色、下拉框浅粉字配浅粉底不可读：v0.3.0 起深色适配块写在浅色基础规则之前，被同特异性后者覆盖，仅 select 的 `!important` 字色残留——深色块移至浅色规则之后，下拉展开选项列表改深酒红底贴合菜单壳（0.3.4「深色下拉可读性」修复的遗留缺陷）。
- 桌宠拖到屏幕顶端之上后开右键菜单三连异常：窗口顶超出工作区使向上扩高算出负值——窗口被压矮下移、菜单底部截断；关菜单还原时又要把窗口移回屏幕外原位，遭系统钳回工作区而高度已恢复，宠物瞬移到屏幕底部。向上增量钳制非负根治；同时修掉旧对称扩宽必然移动窗口 x（渲染层重排滞后 setBounds 一帧）导致的开/关菜单内容左上/右下闪现。
- 待机兜底文案「蕾米埃尔待命中」与状态文案池不一致——统一为「蕾米埃尔待机中~」（两端共 9 处）。

## [0.3.5] — 2026-08-22

### Fixes
- 一键更新弹窗交互重构：更新中/更新完成改为状态文案（不再渲染不可点击的假按钮）；更新进行中禁止点击弹窗外部关闭（仅 ✕ 可关）；失败回到常规视图并保留重试按钮。
- 桌面悬浮模式下更新：成功后自动关闭桌面模式并回落网页宠物——Electron 运行时随更新被移除，继续开着会导致重启后双端皆无宠物；重启后可重新开启（将重新下载运行时）。
- 更新中断健壮性：更新/检查/信息三个路由全量兜底异常，意外中断不再可能以未处理拒绝拖垮宿主；宿主退出时终止进行中的 pnpm/git 子进程，避免孤儿进程把 node_modules 改成半成品导致下次启动崩溃。

## [0.3.4] — 2026-08-22

自 0.3.1 以来的累计变化：

### Features
- 状态气泡升级为两层会话牌叠：顶层显示最高优先级任务，`+N` 背板汇总其余会话；审批 `✓`、提问 `?`、错误 `!` 操作提示，完成绿点保留至处理。
- 桌面悬浮窗与网页牌叠同步；桌面点卡片不跳转会话，`✓` 仍走「允许一次」。
- 气泡双页翻页（状态 / 余额）：单圆点切换 + 滚轮翻页；总开关与「状态」「用量」子开关联动。
- 余额与今日已用：官方余额接口自动刷新 + 数字滚动动画；用量支持「小鲸鱼记账」（免令牌）与「实时·令牌」（平台费用接口，精确）双模式。
- 宠物绘制支持自动检测作品图数量：把 PNG 放进 pics 目录即可，无需改清单。

### Fixes
- 气泡交互：点击/双击/按住/滚轮不再透传给桌宠误触发画画、拖拽、缩放或随机表情；悬浮气泡显示正常指针；状态气泡上滚轮为翻页而非缩放。
- 完成提醒生命周期：会话重跑或被销毁时立即清理提醒（含 one-shot 子 Agent）；当前会话不产生绿点；后台完成与工具报错不再被其他等待/错误会话遮蔽；桌面端确认补齐重试。
- 桌面窗：禁用/隐藏桌宠时彻底释放命中区域，不再隐形拦截桌面点击；右键菜单不再误触画画/缩放；桌面模式激活失败时自动回落网页宠物，不再双端皆无。
- 连续双击画画：立即取消上一轮并开始新一轮（移除画画锁），无需等冷却；重开瞬间清空画面，不闪现上一幅。
- 一键更新修复：registry 安装改用 `pnpm update --latest`（此前依赖声明被精确锁定时会原地重装旧版却提示成功）；更新前自动停止桌面宠物窗并等待进程退出——electron.exe 不再锁住包目录导致 EPERM。
- 余额显示：未知金额显示 “--”（不再是 ¥0.00）；仅开用量且加载中时保留气泡；用量关闭即停止后台轮询。
- 稳定性与一致性：插件重载不再累积连接/监听器/游离节点；快照字段两端对齐；消除余额详情的 HTML 注入面；回合以 disposed 结束不再闪错误卡；深色主题菜单下拉框可读性；随机表情池两端统一为 01–06。

## [0.3.1] — 2026-08-19

基于 0.3.0（PR 作者版本）之上的增量迭代，涵盖最近几轮的完整功能与修复：

### Features
- 贴纸 / 状态映射完善：思考 04、输出 01、调工具 02；assistant/chunk 按 reasoning-delta / text-delta / tool-call-delta 区分贴纸。
- 「等待中 05」接入：ask_user_question（提问回答）、approval/asked（审批等待）、turn/end blocked（回合挂起）。
- 双击画画：粗笔刷沿对角来回揭示作品图（路径带随机、清晰无阴影），完成后「绘制中 → 得意中 → 淡出」；桌面版作品显示在屏幕右上角独立小窗。
- 桌面 / 网页模式切换与设置项同步，desktopMode 持久化（重启后保持上次选择）。
- 内置「版本检查 + 一键更新」：/check 、/update 、/info（GitHub 直连 + HTTP 代理回退）；≥ 0.3.0 的 link 一键 git pull、registry 一键 pnpm update，< 0.3.0 提示彻底卸载重装。
- 设置 → 宠物管理 → 更新：当前版本 / 检查更新 / 一键更新 / 升级说明；右下角更新气泡 + 更新卡片。
- 宠物管理 → 外观 → 添加新宠物：加「开发中」提示（上传功能未完善）。
- 设置卡片精简为仅「是否启用」并对齐标准插件卡片样式。
- 气泡标题在 think/输出阶段按间隔刷新，不再逐 chunk 跳动。
- 移除网页版右键「隐藏桌宠」与右下角 pill 图标。
- 构建时注入版本号（RM_PLUGIN_VERSION）；反馈页显示版本；右键/设置同步。
- 桌面窗桌宠悬停 / 拖动指针（grab / grabbing）。
- README 彻底重写（特性 / 贴纸映射含 GIF 展示 / 安装 / 更新策略 / 桌面模式 / 发布到 npm）。

### Fixes
- 桌面窗 / 页面内关闭后重新开启时气泡与桌宠同步出现。
- 移除虚构的旧行 id dsh-pet-remielle-v2，改为真实历史（0.2.0 前 @dsh-external/dsh-client-ui-pet-remielle、0.2.0–0.3.0 dsh-pet-remielle）。

## [0.3.0] — 2026-08-19

交互与显示打磨（用户实测迭代版）：

### Features
- 右键菜单统一（桌面窗 + 页面内）：粉色气泡同款配色、深浅色自适应；
  菜单固定在角色右上角（窄窗自动回落左/上方，绝不遮挡角色）。
- 右键菜单新增「角色大小」滑块（50%–200%，实时预览），与滚轮缩放、
  设置页滑块三处联动；设置 → 宠物管理 新增同款滑块。
- 页面内宠物菜单新增「桌面悬浮模式」开关，窗外 ⇄ 窗内双向切换
  （host `POST /desktop/{start,stop}`）。

### Fixes
- 表情切换人物大小归一化（charScale 像素分析）：位置 X 0.8px / Y 0px /
  人物高度 0.7px 离散，不再忽大忽小。
- GIF 动画冻结修复：load 重算不再重置 img.src（相同 mood 跳过赋值）。
- 桌面窗 userData 与主壳隔离 + spawn URL cache-busting，杜绝旧页面缓存。
- 窗口模式调整大小不再被强制拉起桌面窗（settings.watch 只在 desktopMode
  翻转时响应）。
- 移除残留右键关闭 handler；ELECTRON_RUN_AS_NODE 环境污染消毒。

### Compatibility
- 63 项测试全绿；已在 fairy（DSH 0.1.0-rc.5）桌面版实测。

# Changelog

## [0.2.0] — 2026-08-19

多宠物化 + 气泡实时汇报 + 桌面悬浮窗口（开源首版）：

### Features
- 多宠物注册表：设置 → 宠物管理（启用/禁用、改名、设为当前、添加宠物）；
  宠物 = `assets/pets/<id>/` 下的 6 张贴纸（GIF），热插拔无需改代码。
- 每贴纸对齐偏移与作品图：`pet-manifest.json`（offsets + pics），
  `assets/pets/<id>/pics/<n>.png` 由双击画画随机弹出。
- 状态气泡：宠物上方粉色气泡显示 message + detail（项目 · 已完成 x/y 步 · 阶段）。
- SSE 实时推送：`/stream` 事件流 + 3s 轮询兜底；脉冲（SUCCESS/ERROR）本地回落。
- 桌面悬浮模式（`desktopMode`，默认开启）：**随包自带 Electron 运行时**
  （vendor/electron-win32-x64，约 221MB）透明置顶窗口，Fairy 桌面版与纯 DSH
  用户行为一致，零外部依赖；窗口支持拖动（位置记忆）、滚轮缩放、双击画画、
  右键关闭；随 host 退出自动关闭。
- 真实 DSH 事件驱动状态机（`session/event` → PetReducer → 消息流）。

### Architecture
- 事件驱动状态机（`session/event` → PetReducer → 类型化协议 → 持久化配置）。
- client bundle 不再内联 GIF（host 端点按需服务），lib/client.js 约 34KiB。

### Tests
- node --test 63 项：协议、状态机、文案、宿主快照、宠物注册表、SSE hub、
  桌面窗口后端探测与生命周期。

### Compatibility
- 已在 DSH 0.1.0-rc.5 源码环境实测：插件加载、宠物管理标签、SSE、
  Qt helper 桌面悬浮全部正常。
